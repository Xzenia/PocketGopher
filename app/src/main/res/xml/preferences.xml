<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:key="monospace_font"
        android:title="Monospace font"
        android:summary="Whether or not the app will use a Monospace or a Serif font."
        android:checked="true" />

    <EditTextPreference
        android:key="font_size"
        android:title="Font Size"
        android:inputType="number" />

    <EditTextPreference
        android:key="line_spacing"
        android:title="Line spacing"
        android:summary="Adjust the spaces between each line of text"
        android:inputType="number" />
</PreferenceScreen>